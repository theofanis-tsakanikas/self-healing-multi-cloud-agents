# UNSAFE FIXTURE — trips WORKFLOW_INLINE_SECRET (a hardcoded credential literal instead of
# ${{ secrets.* }}). Not a real workflow; only fed to the gate in tests.
name: Deploy Pipeline
on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      AWS_SECRET_ACCESS_KEY: EXAMPLE_FIXTURE_LITERAL_NOT_A_REAL_SECRET
      AWS_DEFAULT_REGION: ${{ vars.AWS_DEFAULT_REGION }}
    steps:
      - run: echo deploy
