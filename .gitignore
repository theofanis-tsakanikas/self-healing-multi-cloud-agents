# Bootstrap outputs — contains AWS account IDs, GCP project IDs, Azure subscription IDs
.bootstrap_outputs.json

# Environment secrets
.env
.env.bootstrap
configs/keys/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Test / coverage artifacts
.coverage
.coverage.*
htmlcov/
.pytest_cache/
coverage.xml

# Terraform
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
# NOTE: .terraform.lock.hcl IS committed (HashiCorp guidance — reproducible
# provider versions) for the hand-written bootstrap/ stacks. The generated
# pipeline stack under terraform/ is an agent OUTPUT and is not locked here.
terraform/.terraform.lock.hcl

# Prompt snapshots — working copies, not versioned
agents/prompts_copy/

# Logs
*.log
pipeline_execution.log

# Local TODO / planning / promo notes — not versioned
todo/
promo/
notes/

# macOS
.DS_Store

# === Pre-publish security baseline (added 2026-06-22) ===
PRE-PUBLISH.md
*.pem
*.key
*.p12
*.pfx
*.tfvars

# SSH / private keys
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
*.ppk


# LinkedIn debut/marketing drafts — not part of the public codebase (mirrors promo/)
/LinkedIn/
